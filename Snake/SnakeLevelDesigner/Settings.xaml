<Window x:Class="SnakeLevelDesigner.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnakeLevelDesigner"
        mc:Ignorable="d"
        Title="Settings" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Label>Default level save path</Label>
        <StackPanel Grid.Column="1">
            <Label Name="lSelectedPath">Selected Path</Label>
            <Button Click="SelectPath_OnClick">Select Path</Button>
        </StackPanel>
        
        <Button Grid.Row="1" Click="Save_OnClick" IsDefault="True">Save</Button>
        <Button Grid.Column="1" Click="Cancel_OnClick" Grid.Row="1" IsCancel="True">Cancel</Button>
    </Grid>
</Window>
